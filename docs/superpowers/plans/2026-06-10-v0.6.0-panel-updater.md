# v0.6.0 实现计划：面板重写 + 更新检查 + 关于

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 CustomTkinter 重写状态面板为左侧边栏布局，加入 GitHub 更新检查与关于页，主题跟随系统且 GUI 即时可调。

**Architecture:** 新增 `updater.py`（标准库 urllib 查 GitHub Releases，纯逻辑、可单测）；`config.py` 加 `theme`/`color` 字段与 `save()` 持久化；`panel.py` 全量重写为 CustomTkinter 左侧边栏四页（监控/日志/设置/关于）。`monitor.py` 的 STATS/EVENTS 数据结构不变，面板照旧读取。`tray.py` 的 `panel.open_panel()` 接口不变。

**Tech Stack:** Python 3.11+、CustomTkinter、标准库 urllib.request、pytest、PyInstaller。

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `updater.py` | 新建 | 查 GitHub Releases latest、版本号解析与比较、后台检查 |
| `tests/test_updater.py` | 新建 | updater 纯逻辑单测（打桩 HTTP 响应） |
| `config.py` | 改 | DEFAULTS 加 theme/color；新增 save() 写回 toml |
| `tests/test_config.py` | 新建 | save/load 往返、theme/color 默认值 |
| `panel.py` | 重写 | CustomTkinter 左侧边栏四页 |
| `requirements.txt` | 改 | 加 customtkinter |
| `claude-auto-confirm.spec` | 改 | hiddenimports 加 customtkinter、updater |
| `version.py` / `version_info.txt` | 改 | 升 0.6.0 |
| `README.md` | 改 | 更新功能与文件说明 |

---

## Task 1: updater.py — 版本解析与比较

**Files:**
- Create: `updater.py`
- Test: `tests/test_updater.py`

- [ ] **Step 1: 写失败测试（版本解析+比较）**

```python
# tests/test_updater.py
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import updater


def test_parse_strips_v_prefix():
    assert updater._parse('v0.5.2') == (0, 5, 2)
    assert updater._parse('0.6.0') == (0, 6, 0)


def test_is_newer():
    assert updater._is_newer('v0.5.2', 'v0.5.1') is True
    assert updater._is_newer('v0.5.2', 'v0.5.2') is False
    assert updater._is_newer('v0.5.1', 'v0.5.2') is False
    assert updater._is_newer('v0.6.0', 'v0.5.9') is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_updater.py -v`
Expected: FAIL（No module named 'updater'）

- [ ] **Step 3: 写最小实现**

```python
# updater.py
"""GitHub Releases 更新检查：查 latest release tag，与当前版本比较。
用标准库 urllib，不引入网络依赖。所有网络调用静默失败（返回 None），
不打断主流程。"""
import json
import threading
import urllib.request
from applog import log

API_URL = ('https://api.github.com/repos/'
           'huangzhuo-ai/claude-auto-confirm/releases/latest')
RELEASES_PAGE = 'https://github.com/huangzhuo-ai/claude-auto-confirm/releases'
_TIMEOUT = 6  # 秒，网络慢时不拖死后台线程


def _parse(tag: str) -> tuple:
    """'v0.5.2' / '0.5.2' → (0, 5, 2)。非法段记 0，容忍 'v0.6' 之类。"""
    parts = tag.lstrip('vV').split('.')
    out = []
    for p in parts:
        try:
            out.append(int(p))
        except ValueError:
            out.append(0)
    return tuple(out)


def _is_newer(latest: str, current: str) -> bool:
    """latest 是否严格新于 current。"""
    return _parse(latest) > _parse(current)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_updater.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add updater.py tests/test_updater.py
git commit -m "feat: updater 版本解析与比较"
```

---

## Task 2: updater.py — 查 latest release（打桩 HTTP）

**Files:**
- Modify: `updater.py`
- Test: `tests/test_updater.py`

- [ ] **Step 1: 追加失败测试（monkeypatch urlopen）**

```python
# 追加到 tests/test_updater.py
import io
import json as _json


class _FakeResp(io.BytesIO):
    def __enter__(self): return self
    def __exit__(self, *a): self.close()


def _fake_urlopen(payload):
    def _open(url, timeout=None):
        return _FakeResp(_json.dumps(payload).encode())
    return _open


def test_get_latest_version_ok(monkeypatch):
    monkeypatch.setattr(updater.urllib.request, 'urlopen',
                        _fake_urlopen({'tag_name': 'v0.6.0'}))
    assert updater.get_latest_version() == 'v0.6.0'


def test_get_latest_version_network_fail(monkeypatch):
    def _boom(url, timeout=None):
        raise OSError('no network')
    monkeypatch.setattr(updater.urllib.request, 'urlopen', _boom)
    assert updater.get_latest_version() is None


def test_check_returns_tuple(monkeypatch):
    monkeypatch.setattr(updater.urllib.request, 'urlopen',
                        _fake_urlopen({'tag_name': 'v0.6.0'}))
    has, latest = updater.check('v0.5.2')
    assert has is True and latest == 'v0.6.0'
    has2, _ = updater.check('v0.6.0')
    assert has2 is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_updater.py -v`
Expected: FAIL（get_latest_version / check 未定义）

- [ ] **Step 3: 实现 get_latest_version 与 check**

```python
# 追加到 updater.py
def get_latest_version() -> str | None:
    """查 GitHub latest release 的 tag_name。任何失败返回 None（静默）。"""
    try:
        req = urllib.request.Request(
            API_URL, headers={'Accept': 'application/vnd.github+json',
                              'User-Agent': 'claude-auto-confirm'})
        with urllib.request.urlopen(req, timeout=_TIMEOUT) as r:
            data = json.loads(r.read().decode())
        return data.get('tag_name')
    except Exception as e:
        log(f'[updater] 查询失败: {e}')
        return None


def check(current: str) -> tuple[bool, str | None]:
    """返回 (有新版, 最新tag)。查询失败返回 (False, None)。"""
    latest = get_latest_version()
    if not latest:
        return False, None
    return _is_newer(latest, current), latest
```

注意：测试里 monkeypatch 的是 `urllib.request.urlopen`，实现中需用
`urllib.request.urlopen(req, ...)`（req 是 Request 对象，fake 的签名
`(url, timeout=None)` 兼容位置参数）。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_updater.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add updater.py tests/test_updater.py
git commit -m "feat: updater 查 GitHub latest release"
```

---

## Task 3: updater.py — 后台检查

**Files:**
- Modify: `updater.py`

- [ ] **Step 1: 实现后台检查（无需新测试，纯线程封装）**

```python
# 追加到 updater.py
def check_in_background(current: str, on_update) -> None:
    """后台线程查更新，有新版时回调 on_update(latest_tag)。
    on_update 须自行处理线程安全（如需操作 GUI 用 widget.after）。"""
    def run():
        has, latest = check(current)
        if has and latest:
            log(f'[updater] 发现新版 {latest}')
            try:
                on_update(latest)
            except Exception as e:
                log(f'[updater] 回调失败: {e}')
    threading.Thread(target=run, daemon=True, name='updater').start()
```

- [ ] **Step 2: 全套测试确认无回归**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add updater.py
git commit -m "feat: updater 后台检查线程"
```

---

## Task 4: config.py — theme/color 字段 + save()

**Files:**
- Modify: `config.py`
- Test: `tests/test_config.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_config.py
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import config


def test_defaults_have_theme_color():
    d = config.DEFAULTS
    assert d['theme'] == 'system'
    assert d['color'] == 'blue'


def test_save_then_load_roundtrip(tmp_path, monkeypatch):
    p = tmp_path / 'config.toml'
    monkeypatch.setattr(config, '_config_path', lambda: p)
    cfg = config.load()
    cfg['theme'] = 'dark'
    cfg['color'] = 'green'
    config.save(cfg)
    assert p.exists()
    reloaded = config.load()
    assert reloaded['theme'] == 'dark'
    assert reloaded['color'] == 'green'
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_config.py -v`
Expected: FAIL（theme 不在 DEFAULTS / save 未定义）

- [ ] **Step 3: 改 config.py**

DEFAULTS 加两个字段：

```python
DEFAULTS = {
    'scan_interval': 1.5,
    'waiting_notify_seconds': 15,
    'ignored_titles': [],
    'theme': 'system',   # system | dark | light
    'color': 'blue',     # blue | green | dark-blue
}
```

文件顶部 import 加 `import tomli_w`（写 toml）。**若环境无 tomli_w，改用手写最简 toml writer**（见下方备选）。先尝试标准做法：

```python
def save(cfg: dict) -> bool:
    """把配置写回 config.toml。成功 True。"""
    p = _config_path()
    try:
        import tomli_w
        with p.open('wb') as f:
            tomli_w.dump(cfg, f)
        return True
    except ImportError:
        # 无 tomli_w：手写最简 toml（本项目配置仅含 str/num/list[str]）
        lines = []
        for k, v in cfg.items():
            if isinstance(v, str):
                lines.append(f'{k} = "{v}"')
            elif isinstance(v, bool):
                lines.append(f'{k} = {"true" if v else "false"}')
            elif isinstance(v, (int, float)):
                lines.append(f'{k} = {v}')
            elif isinstance(v, list):
                items = ', '.join(f'"{x}"' for x in v)
                lines.append(f'{k} = [{items}]')
        p.write_text('\n'.join(lines) + '\n', encoding='utf-8')
        return True
    except Exception as e:
        print(f'[WARN] 保存 config.toml 失败: {e}')
        return False
```

注意：`load()` 已有 `_config_path()`，测试靠 monkeypatch 它来重定向到 tmp_path。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_config.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add config.py tests/test_config.py
git commit -m "feat: config 加 theme/color 字段与 save()"
```

---

## Task 5: 安装 customtkinter + 更新 requirements/spec

**Files:**
- Modify: `requirements.txt`, `claude-auto-confirm.spec`

- [ ] **Step 1: 安装**

Run: `pip install customtkinter`
Expected: Successfully installed customtkinter-x.x.x

- [ ] **Step 2: 验证可导入并查版本**

Run: `python -c "import customtkinter as c; print(c.__version__)"`
Expected: 打印版本号（如 5.2.2）

- [ ] **Step 3: 加进 requirements.txt**

在文件末尾追加一行（版本号用上一步查到的实际值）：

```
customtkinter==5.2.2
```

- [ ] **Step 4: 改 spec 的 collect_all 与 hiddenimports**

`claude-auto-confirm.spec` 中：
- `for pkg in ('win11toast', 'uiautomation', 'pystray'):` 改为
  `for pkg in ('win11toast', 'uiautomation', 'pystray', 'customtkinter'):`
- hiddenimports 那行末尾加 `'updater'`：
  `hiddenimports += ['panel', 'tray', 'config', 'terminal', 'applog', 'version', 'autostart', 'singleton', 'updater']`

- [ ] **Step 5: 提交**

```bash
git add requirements.txt claude-auto-confirm.spec
git commit -m "build: 加 customtkinter 依赖与打包配置"
```

---

## Task 6: 重写 panel.py — 骨架与侧边栏导航

**Files:**
- Rewrite: `panel.py`

说明：panel.py 全量重写。保持对外接口 `open_panel()` 不变（tray 调用它）。
旧的 `_STATE_ICONS` / `_ACTION_LABELS` 文案沿用。本任务先搭出左侧边栏 +
四个空白页 + 页面切换，后续任务往各页填内容。因 GUI 无法单测，验收靠
「能导入 + 能起窗不报错」。

- [ ] **Step 1: 写新 panel.py 骨架**

```python
"""
状态面板（CustomTkinter）：左侧边栏导航 4 个页面——监控/日志/设置/关于。
通过 open_panel() 单例开窗，关掉面板不影响后台监控。
主题跟随系统，设置页可即时切换明暗与主题色。
"""
import time
import threading
import webbrowser
import tkinter as tk
import customtkinter as ctk

import monitor
import config
import updater
from version import __version__

_STATE_ICONS = {
    'running': '🔵 运行中', 'idle': '🟡 空闲', 'idle_notified': '🟠 空闲已通知',
    'confirmed': '✅ 已自动确认', 'prompt': '🔴 需处理', 'error': '❌ 错误',
    'unknown': '⚠️ 未知确认框', 'ignored': '⚫ 已忽略',
}
_ACTION_LABELS = {
    'auto_yes': '✅ 自动确认', 'notify': '🔔 已通知', 'error': '❌ 错误通知',
    'unknown': '⚠️ 未知框(已记录)', 'idle': '🟠 空闲通知',
}

_COLOR_MAP = {'blue': 'blue', 'green': 'green', 'dark-blue': 'dark-blue'}
_THEME_MAP = {'system': 'system', 'dark': 'dark', 'light': 'light'}

_panel_thread: threading.Thread | None = None
_panel_lock = threading.Lock()


def open_panel():
    """从托盘菜单调用：若面板已开则忽略，否则新建。"""
    global _panel_thread
    with _panel_lock:
        if _panel_thread is not None and _panel_thread.is_alive():
            return
        _panel_thread = threading.Thread(target=_run_panel, daemon=True, name='panel')
        _panel_thread.start()


def _run_panel():
    cfg = config.load()
    ctk.set_appearance_mode(_THEME_MAP.get(cfg.get('theme', 'system'), 'system'))
    ctk.set_default_color_theme(_COLOR_MAP.get(cfg.get('color', 'blue'), 'blue'))

    root = ctk.CTk()
    root.title('Claude Auto-Yes · 状态面板')
    root.geometry('900x560')

    # ── 左侧边栏 ──
    sidebar = ctk.CTkFrame(root, width=160, corner_radius=0)
    sidebar.pack(side='left', fill='y')
    sidebar.pack_propagate(False)

    ctk.CTkLabel(sidebar, text='⚡ Auto-Yes',
                 font=ctk.CTkFont(size=16, weight='bold')).pack(pady=(18, 16))

    content = ctk.CTkFrame(root, corner_radius=0, fg_color='transparent')
    content.pack(side='left', fill='both', expand=True)

    pages = {}
    for name in ('监控', '日志', '设置', '关于'):
        pages[name] = ctk.CTkFrame(content, fg_color='transparent')

    def show(name):
        for p in pages.values():
            p.pack_forget()
        pages[name].pack(fill='both', expand=True, padx=16, pady=16)

    for name in ('监控', '日志', '设置', '关于'):
        ctk.CTkButton(sidebar, text=name, anchor='w',
                      command=lambda n=name: show(n)).pack(fill='x', padx=10, pady=3)

    status_lbl = ctk.CTkLabel(sidebar, text='', font=ctk.CTkFont(size=11))
    status_lbl.pack(side='bottom', pady=12)

    # 各页内容由后续任务填充：
    _build_monitor_page(pages['监控'])
    _build_log_page(pages['日志'])
    _build_settings_page(pages['设置'], root)
    _build_about_page(pages['关于'])

    show('监控')

    def _refresh():
        if monitor.PAUSED.is_set():
            status_lbl.configure(text='⏸ 已暂停')
        else:
            status_lbl.configure(text=f'监控中 · {monitor.STATS["windows"]} 个终端')
        _refresh_monitor()
        _refresh_log()
        root.after(1000, _refresh)

    root.after(0, _refresh)
    root.mainloop()
```

下面这些函数本任务先写成空占位（后续任务实现）：

```python
def _build_monitor_page(frame): pass
def _build_log_page(frame): pass
def _build_settings_page(frame, root): pass
def _build_about_page(frame): pass
def _refresh_monitor(): pass
def _refresh_log(): pass
```

- [ ] **Step 2: 验证可导入**

Run: `python -c "import panel; print('panel import OK')"`
Expected: panel import OK

- [ ] **Step 3: 提交**

```bash
git add panel.py
git commit -m "feat: panel CustomTkinter 骨架与侧边栏导航"
```

---

## Task 7: panel.py — 监控页（表格 + 策略按钮 + 跳转）

**Files:**
- Modify: `panel.py`

说明：用 `tkinter.ttk.Treeview` 嵌进 CTkFrame（CustomTkinter 无原生表格，
ttk.Treeview 是社区通行做法）。表格读 `monitor._win_state`，逻辑与旧面板一致。

- [ ] **Step 1: 实现 _build_monitor_page 与 _refresh_monitor**

替换 panel.py 中的占位函数。需要模块级保存 tree 引用供 refresh 使用：

```python
from tkinter import ttk

_tree = None  # 模块级，_refresh_monitor 用


def _build_monitor_page(frame):
    global _tree
    bar = ctk.CTkFrame(frame, fg_color='transparent')
    bar.pack(fill='x', pady=(0, 8))
    ctk.CTkLabel(bar, text='选中行后设策略：').pack(side='left', padx=(0, 6))
    for label, policy in [('自动确认', 'auto'), ('仅通知', 'notify'), ('忽略', 'ignore')]:
        ctk.CTkButton(bar, text=label, width=72,
                      command=lambda p=policy: _set_selected_policy(p)
                      ).pack(side='left', padx=3)

    cols = ('kind', 'title', 'policy', 'state', 'detail', 'ts')
    _tree = ttk.Treeview(frame, columns=cols, show='headings',
                         selectmode='browse', height=12)
    for col, head, w in [('kind', '类型', 90), ('title', '标题', 240),
                         ('policy', '策略', 70), ('state', '状态', 130),
                         ('detail', '详情', 180), ('ts', '时间', 80)]:
        _tree.heading(col, text=head)
        _tree.column(col, width=w, anchor='w')
    _tree.pack(fill='both', expand=True)

    def _on_dbl(_e):
        item = _tree.focus()
        if item:
            try:
                monitor._bring_to_front(int(item))
            except Exception:
                pass
    _tree.bind('<Double-1>', _on_dbl)


def get_policy_label(hwnd):
    return {'auto': '自动', 'notify': '仅通知', 'ignore': '忽略'}.get(
        monitor.get_policy(hwnd), '自动')


def _set_selected_policy(policy):
    if _tree is None:
        return
    item = _tree.focus()
    if not item:
        return
    try:
        hwnd = int(item)
        monitor.set_policy(hwnd, policy)
    except Exception:
        pass


def _refresh_monitor():
    if _tree is None:
        return
    snap = dict(monitor._win_state)
    existing = set(_tree.get_children())
    seen = set()
    for hwnd, row in snap.items():
        iid = str(hwnd)
        seen.add(iid)
        vals = (row.get('kind', ''), row.get('title', '')[:50],
                get_policy_label(hwnd),
                _STATE_ICONS.get(row.get('state', ''), row.get('state', '')),
                row.get('detail', '')[:60],
                time.strftime('%H:%M:%S', time.localtime(row.get('ts', 0))))
        if iid in existing:
            _tree.item(iid, values=vals)
        else:
            _tree.insert('', 'end', iid=iid, values=vals)
    for iid in existing - seen:
        _tree.delete(iid)
```

- [ ] **Step 2: 验证可导入 + 起窗烟测**

Run: `python -c "import panel; print('OK')"`
Expected: OK

（可选手动：临时跑 `python -c "import panel; panel._run_panel()"` 看窗口，
确认监控页表格出现后关掉。）

- [ ] **Step 3: 提交**

```bash
git add panel.py
git commit -m "feat: panel 监控页表格与策略控制"
```

---

## Task 8: panel.py — 日志页

**Files:**
- Modify: `panel.py`

- [ ] **Step 1: 实现 _build_log_page 与 _refresh_log**

```python
_log_box = None
_log_snapshot = [None]


def _build_log_page(frame):
    global _log_box
    ctk.CTkLabel(frame, text='事件日志',
                 font=ctk.CTkFont(size=14, weight='bold')).pack(anchor='w', pady=(0, 6))
    _log_box = ctk.CTkTextbox(frame, font=ctk.CTkFont(family='Consolas', size=12))
    _log_box.pack(fill='both', expand=True)
    _log_box.configure(state='disabled')


def _refresh_log():
    if _log_box is None:
        return
    events = list(monitor.EVENTS)
    if events == _log_snapshot[0]:
        return
    _log_snapshot[0] = events
    _log_box.configure(state='normal')
    _log_box.delete('1.0', 'end')
    for ev in reversed(events):
        ts = time.strftime('%H:%M:%S', time.localtime(ev['ts']))
        action = _ACTION_LABELS.get(ev['action'], ev['action'])
        _log_box.insert('end',
            f"{ts}  {action:<12}  [{ev['kind']}] {ev['title'][:35]}  {ev['detail'][:60]}\n")
    _log_box.configure(state='disabled')
```

- [ ] **Step 2: 验证可导入**

Run: `python -c "import panel; print('OK')"`
Expected: OK

- [ ] **Step 3: 提交**

```bash
git add panel.py
git commit -m "feat: panel 日志页"
```

---

## Task 9: panel.py — 设置页（开关 + 外观即时切换）

**Files:**
- Modify: `panel.py`

- [ ] **Step 1: 实现 _build_settings_page**

```python
def _build_settings_page(frame, root):
    cfg = config.load()

    # ── 全局开关 ──
    ctk.CTkLabel(frame, text='监控开关',
                 font=ctk.CTkFont(size=14, weight='bold')).pack(anchor='w')

    pause_sw = ctk.CTkSwitch(frame, text='暂停监控')
    pause_sw.pack(anchor='w', pady=4)
    if monitor.PAUSED.is_set():
        pause_sw.select()

    def _toggle_pause():
        if pause_sw.get():
            monitor.PAUSED.set()
        else:
            monitor.PAUSED.clear()
    pause_sw.configure(command=_toggle_pause)

    dry_sw = ctk.CTkSwitch(frame, text='干跑（只检测不发键）')
    dry_sw.pack(anchor='w', pady=4)
    if monitor.DRY_RUN:
        dry_sw.select()
    dry_sw.configure(command=lambda: setattr(monitor, 'DRY_RUN', bool(dry_sw.get())))

    import autostart
    auto_sw = ctk.CTkSwitch(frame, text='开机自启')
    auto_sw.pack(anchor='w', pady=4)
    if autostart.is_enabled():
        auto_sw.select()

    def _toggle_auto():
        want_on = bool(auto_sw.get())
        autostart.toggle()
        ok = autostart.is_enabled()
        if ok != want_on:
            auto_sw.deselect() if not ok else auto_sw.select()
        if want_on and not ok:
            from tkinter import messagebox
            messagebox.showwarning('开机自启被拦截',
                '写入失败，疑被安全软件/系统管控拦截。请加白名单后重试。')
    auto_sw.configure(command=_toggle_auto)

    # ── 外观 ──
    ctk.CTkLabel(frame, text='外观',
                 font=ctk.CTkFont(size=14, weight='bold')).pack(anchor='w', pady=(16, 0))

    _THEME_LABELS = {'跟随系统': 'system', '深色': 'dark', '浅色': 'light'}
    _THEME_REV = {v: k for k, v in _THEME_LABELS.items()}
    _COLOR_LABELS = {'蓝': 'blue', '绿': 'green', '深蓝': 'dark-blue'}
    _COLOR_REV = {v: k for k, v in _COLOR_LABELS.items()}

    row1 = ctk.CTkFrame(frame, fg_color='transparent')
    row1.pack(anchor='w', pady=6, fill='x')
    ctk.CTkLabel(row1, text='明暗：', width=60, anchor='w').pack(side='left')

    def _on_theme(choice):
        mode = _THEME_LABELS[choice]
        ctk.set_appearance_mode(mode)
        cfg['theme'] = mode
        config.save(cfg)

    theme_menu = ctk.CTkOptionMenu(row1, values=list(_THEME_LABELS.keys()),
                                   command=_on_theme)
    theme_menu.set(_THEME_REV.get(cfg.get('theme', 'system'), '跟随系统'))
    theme_menu.pack(side='left')

    row2 = ctk.CTkFrame(frame, fg_color='transparent')
    row2.pack(anchor='w', pady=6, fill='x')
    ctk.CTkLabel(row2, text='主题色：', width=60, anchor='w').pack(side='left')

    def _on_color(choice):
        col = _COLOR_LABELS[choice]
        cfg['color'] = col
        config.save(cfg)
        from tkinter import messagebox
        messagebox.showinfo('主题色', '主题色将在下次打开面板时完整生效。')

    color_menu = ctk.CTkOptionMenu(row2, values=list(_COLOR_LABELS.keys()),
                                   command=_on_color)
    color_menu.set(_COLOR_REV.get(cfg.get('color', 'blue'), '蓝'))
    color_menu.pack(side='left')
```

注意：`set_appearance_mode` 可运行时即时切换；`set_default_color_theme`
（主题色）CustomTkinter 限制为须在建 widget 前调用，故主题色改动提示
「下次打开生效」，明暗则立即生效。这是该库的已知限制，不是缺陷。

- [ ] **Step 2: 验证可导入**

Run: `python -c "import panel; print('OK')"`
Expected: OK

- [ ] **Step 3: 提交**

```bash
git add panel.py
git commit -m "feat: panel 设置页——开关与外观即时切换"
```

---

## Task 10: panel.py — 关于页 + 更新检查接入

**Files:**
- Modify: `panel.py`

- [ ] **Step 1: 实现 _build_about_page，并在 _run_panel 启动后台检查**

```python
_about_status = None  # 模块级 CTkLabel，供后台回调更新


def _build_about_page(frame):
    global _about_status
    ctk.CTkLabel(frame, text='Claude Auto-Yes',
                 font=ctk.CTkFont(size=20, weight='bold')).pack(anchor='w', pady=(0, 4))
    ctk.CTkLabel(frame, text=f'版本 v{__version__}').pack(anchor='w')
    ctk.CTkLabel(frame, text='作者：huangzhuo').pack(anchor='w', pady=(8, 0))

    link = ctk.CTkLabel(frame, text='GitHub 项目主页',
                        text_color=('blue', '#6db3f2'), cursor='hand2')
    link.pack(anchor='w', pady=(8, 0))
    link.bind('<Button-1>',
              lambda _e: webbrowser.open(updater.RELEASES_PAGE.rsplit('/', 1)[0]))

    _about_status = ctk.CTkLabel(frame, text='')
    _about_status.pack(anchor='w', pady=(16, 0))

    def _manual_check():
        _about_status.configure(text='检查中…')
        def run():
            has, latest = updater.check(__version__)
            if has and latest:
                _about_status.configure(
                    text=f'发现新版 {latest}，点此前往下载', text_color=('blue', '#6db3f2'))
                _about_status.configure(cursor='hand2')
                _about_status.bind('<Button-1>',
                    lambda _e: webbrowser.open(updater.RELEASES_PAGE))
            elif latest:
                _about_status.configure(text='已是最新版', text_color=('gray', 'gray'))
            else:
                _about_status.configure(text='检查失败（网络问题）', text_color=('gray', 'gray'))
        threading.Thread(target=run, daemon=True).start()

    ctk.CTkButton(frame, text='检查更新', command=_manual_check).pack(anchor='w', pady=(8, 0))


def _on_update_found(latest):
    """后台启动检查的回调：在关于页状态标签提示。"""
    if _about_status is not None:
        try:
            _about_status.configure(
                text=f'发现新版 {latest}，去「关于」页查看', text_color=('blue', '#6db3f2'))
        except Exception:
            pass
```

在 `_run_panel` 的 `show('监控')` 之后、`_refresh` 定义之前，加一行启动后台检查：

```python
    updater.check_in_background(__version__, _on_update_found)
```

- [ ] **Step 2: 验证可导入**

Run: `python -c "import panel; print('OK')"`
Expected: OK

- [ ] **Step 3: 全套测试无回归**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add panel.py
git commit -m "feat: panel 关于页与更新检查接入"
```

---

## Task 11: 升版本 + 打包 + 真机实测

**Files:**
- Modify: `version.py`, `version_info.txt`, `README.md`

- [ ] **Step 1: 升版本号到 0.6.0**

`version.py`：`__version__ = '0.6.0'`
`version_info.txt`：注释、filevers/prodvers `(0, 6, 0, 0)`、FileVersion/ProductVersion `'0.6.0'`（共 5 处，参照上一版做法逐处改）。

- [ ] **Step 2: 更新 README.md**

功能列表加「更新检查」「关于页」「面板美化（CustomTkinter）」；文件表加 `updater.py`。

- [ ] **Step 3: 停掉运行中实例（避免占用 exe）**

Run (PowerShell): `Stop-Process -Name claude-auto-confirm -Force -ErrorAction SilentlyContinue`

- [ ] **Step 4: 打包**

Run: `python -m PyInstaller claude-auto-confirm.spec --noconfirm`
Expected: Build complete

- [ ] **Step 5: 验证 exe 版本与可启动**

Run (PowerShell): `(Get-Item dist\claude-auto-confirm.exe).VersionInfo.FileVersion`
Expected: 0.6.0

手动：双击 dist\claude-auto-confirm.exe → 托盘 → 打开面板 → 逐页点（监控/日志/设置/关于）→ 设置页切明暗看是否即时变 → 关于页点「检查更新」应显示「已是最新版」（因 GitHub 上是 v0.5.2 < 0.6.0，实际会显示已最新；若想测到「发现新版」可临时把 __version__ 当 0.5.1 跑 dev）。

- [ ] **Step 6: 提交并推送**

```bash
git add version.py version_info.txt README.md
git commit -m "feat: 面板重写+更新检查+关于 v0.6.0"
git push
```

- [ ] **Step 7: 发布 v0.6.0 release**

Run (PowerShell): `& "$env:ProgramFiles\GitHub CLI\gh.exe" release create v0.6.0 dist\claude-auto-confirm.exe --repo huangzhuo-ai/claude-auto-confirm --title "v0.6.0 — 面板重写+更新检查" --notes "..."`

---

## 自检结果

- **Spec 覆盖**：面板 4 页（Task 6-10）✓；更新检查自动+手动（Task 3/10）✓；
  关于页字段（Task 10）✓；主题跟随系统+GUI 切换（Task 9）✓；config 持久化（Task 4）✓；
  打包+release（Task 11）✓。
- **已知取舍**：主题色（color）受 CustomTkinter 限制无法运行时完整切换，
  改为「下次打开生效」并明确提示用户——明暗（appearance_mode）可即时切。
  此取舍已在 Task 9 注明，符合「诚实告知」原则。
- **类型一致**：`updater.check()` 返回 `(bool, str|None)`，panel 各处按此解构 ✓；
  `config.save(cfg)` 签名一致 ✓。

